{
  "watch": ["src", "prisma"],
  "ext": "ts,json",
  "ignore": ["src/logs/*", "uploads/*", "backups/*"],
  "exec": "ts-node -r tsconfig-paths/register src/server.ts",
  "env": {
    "NODE_ENV": "development"
  }
}
