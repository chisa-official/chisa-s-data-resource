# =================================================================
# 学生端前端 - 生产环境配置（Netlify 部署）
# =================================================================
# 注意：
#   1. 先部署 Render 后端，拿到后端 Service URL（例如 https://student-mgmt-api.onrender.com）
#   2. 将下面的 VITE_API_BASE_URL 和 VITE_WS_URL 替换成你的实际地址
#   3. 再执行 npm run build，把 student-web/dist 文件夹拖拽到 https://app.netlify.com/drop
#   4. Netlify 分配好域名后，回到 Render 后端管理面板，
#      将 CORS_ORIGIN 环境变量设置为 Netlify 分配的域名（多域名用逗号分隔），
#      例如：https://你的学生端域名.netlify.app,https://你的后台端域名.netlify.app
# =================================================================

# API 基础地址：填 Render 后端地址 + /api
# 示例：VITE_API_BASE_URL=https://student-mgmt-api.onrender.com/api
VITE_API_BASE_URL=

# WebSocket 地址：填 Render 后端地址（不带 /api，socket.io 会自动连 /socket.io/）
# 示例：VITE_WS_URL=https://student-mgmt-api.onrender.com
VITE_WS_URL=
